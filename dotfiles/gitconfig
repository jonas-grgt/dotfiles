[alias]
    l = log --graph --pretty=format:'%C(yellow)%h%Creset -%C(bold red)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
    f = fetch -p
    s = status -sb
    sl = stash list --pretty=format:'%Cblue%gd%Cred: %C(yellow)%s'
    co = checkout
    plo = pull origin
    pso = push origin
    ci = commit -a -m
    br = branch
    hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
    type = cat-file -t
    dump = cat-file -p
[user]
    name = Jonas Geiregat
    email = jonas.geiregat@mateco.eu
[pager]
    log = true
[merge]
    conflictstyle = diff3
[rerere]
	enabled = true
