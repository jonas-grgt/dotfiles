if [[ -f ~/.zshrc.mac ]]; then
    source ~/.zshrc.mac;
fi
if [[ -f ~/.zsh.aliases ]]; then
    source ~/.zsh.aliases;
fi
source $ZSH/oh-my-zsh.sh
if [[ -f ~/.p10k.zsh ]]; then
    source ~/.p10k.zsh;
fi
